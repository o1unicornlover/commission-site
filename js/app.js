/*
  Future home for App.init().
  Stage 1 is intentionally conservative: the current site still runs through
  legacy-app.js so nothing breaks while we split features gradually.
*/
window.App = window.App || {};
